# Target Local Repository Path

Requested destination:

```text
/Users/mohamedali/Library/CloudStorage/OneDrive-LeedsBeckettUniversity/Work/GITHUB REPO/INQETRA
```

The generated ChatGPT sandbox cannot mount or write to that macOS path. After downloading/extracting this pack, run:

```bash
bash scripts/install_to_target.sh
```

The script copies **the contents of this pack** into the target directory and does not delete unrelated files.
